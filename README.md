# Palette Generator

This is for now more or less a playground. I'm trying out if it's possible to automatically generate UI color palettes.

The goal is to find a formular that can later be implemented using CSS `@function`s and mixins - or Sass, if these proposals don't make the cut - to automatically generate palettes in your stylesheets.
